{
  "name": "awesome-agent-infra",
  "description": "Awesome Agent Infrastructure — 42 battle-tested, zero-dependency TypeScript packages for building reliable AI agents. Memory, validation, code graphs, fuzzy patching, MCTS reasoning, POMDP planning, transactional filesystems, self-evolution, and more.",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["packages/*", "demo/*"],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "integration": "turbo integration",
    "typecheck": "turbo typecheck",
    "lint": "biome check --error-on-warnings .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "check": "turbo typecheck && biome check . && turbo test && turbo build",
    "docs": "typedoc",
    "docs:watch": "typedoc --watch",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.0",
    "@types/node": "^26.1.2",
    "bun-types": "^1.3.14",
    "turbo": "^2.8.0",
    "typedoc": "^0.28.0",
    "typescript": "^5.8.0"
  }
}
